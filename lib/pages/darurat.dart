import 'package:flutter/material.dart';

class DaruratPage extends StatelessWidget {
  const DaruratPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("darurat", style: TextStyle(fontSize: 24)),
    );
  }
}
